const urls = {
  dashboard: {
    getLabel: () => "Dashboard",
    getHref: () => "/dashboard",
  },
  session: {
    getLabel: () => "Session",
    getHref: () => "/session",
  },
};

export default urls;
